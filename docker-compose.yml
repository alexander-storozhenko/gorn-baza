version: "3.1"


volumes:
  db: {}

services:
  app:
#    image: postgres:14
    command: rails server -b 0.0.0.0
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    ports:
      - 3000:3000
  db:
    image: postgres:14
    volumes:
      - db:/var/lib/postgresql/data
    ports:
      - 5432:5432
